#include <stdio.h>
#include <string.h>
#include <stdlib.h>

void kaprekarNumbers(int p, int q) {
    int found = 0;

    for (long long i = p; i <= q; i++) {
        long long sq = i * i;
        char sq_str[50];
        sprintf(sq_str, "%lld", sq);

        int len = strlen(sq_str);
        long long temp = i;
        int d = 0;
        while (temp > 0) {
            d++;
            temp /= 10;
        }

        char left[50] = "0";
        char right[50] = "0";

        if (d <= len) {
            strcpy(right, sq_str + len - d);
        } else {
            strcpy(right, sq_str);
        }

        if (len - d > 0) {
            strncpy(left, sq_str, len - d);
            left[len - d] = '\0';
        }

        long long left_val = atoll(left);
        long long right_val = atoll(right);

        if (left_val + right_val == i) {
            printf("%lld ", i);
            found = 1;
        }
    }

    if (!found) {
        printf("INVALID RANGE");
    }
    printf("\n");
}

int main() {
    int p, q;
    scanf("%d %d", &p, &q);
    kaprekarNumbers(p, q);
    return 0;
}
